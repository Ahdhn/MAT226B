function [L, flag, k] = spcholesky(A, nnzmax, tlimit)
    n = length(A);
    L = tril(A); 
        
    for k =1:n
        tic; 
        
        time = toc;
        if 
        if time > tlimit
            flag = 2;
            break;
        end
    end
    flag =0;
end 