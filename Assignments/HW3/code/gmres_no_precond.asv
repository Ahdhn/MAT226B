function [rho, x, numMattVec, iter] = gmres_no_precond(A, b,x0, tol)
    N = size(A,1);
    [x,flag,relres,iter,rho] = gmres(A,b,[],tol,N,[],[],x0);
    if flag != 0
        printf('GMRES without p')
    end
end 





